[pytest]
; Suite canonica del repo: `pytest` a secas desde la raiz corre TODO
; (ocr/tests, video/tests y persistencia/tests; el simulador no tiene tests).
testpaths = ocr/tests video/tests persistencia/tests
